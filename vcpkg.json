{
  "name": "budouxpp",
  "version-string": "0.1.0",
  "dependencies": [
    "glaze",
    "unordered-dense"
  ],
  "features": {
    "tests": {
      "description": "Build the test target and enable its test-only dependency.",
      "dependencies": [
        "catch2"
      ]
    },
    "example": {
      "description": "Build the example executable and enable its logging dependency.",
      "dependencies": []
    },
    "benchmark": {
      "description": "Build the benchmark executable.",
      "dependencies": []
    }
  }
}
